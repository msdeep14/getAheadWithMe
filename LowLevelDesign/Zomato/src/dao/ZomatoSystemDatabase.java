package dao;

/**
 * In memory database to keep all the details.
 * */
public class ZomatoSystemDatabase {
    /**
     * 1. Map for restaurants -- Map(restaurantName, Restaurant)
     * 2. Map for FoodItems -- Map(String, List(restaurants))
     * 3.
     * */
}
